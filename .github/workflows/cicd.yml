name: CI/CD Pipeline

on:
  push:
    branches:
      - main
jobs:

  action-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Print index.html
        run: cat index.html


